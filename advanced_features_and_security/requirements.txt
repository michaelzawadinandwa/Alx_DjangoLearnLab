# Django and core dependencies
Django==6.0
Pillow==10.0.0  # Required for ImageField support

# Development dependencies (optional)
django-debug-toolbar==4.1.0  # For debugging in development
django-extensions==3.2.3  # Additional Django management commands

# Production dependencies
gunicorn==21.2.0  # WSGI HTTP server for production
whitenoise==6.5.0  # Static files handling in production

# Security and testing
django-cors-headers==4.2.0  # Handle CORS headers
python-decouple==3.8  # Environment variable management
